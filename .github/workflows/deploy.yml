name: Deploy main branch

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: deploy
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm ci
      - name: serverless deploy
        uses: serverless/github-action@v3.1
        with:
          args: deploy
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      # - name: Update Swagger Website
      # - uses: actions/checkout@v1
      # - run: |
      #     sls downloadDocumentation --outputFileName=docs.json
      #     aws s3api put-object --bucket colkie-swagger-production --key apis/colkie --content-type "application/json" --body docs.json
      #     wget https://github.com/swagger-api/swagger-ui/archive/refs/tags/v4.1.3.tar.gz
      #     tar -zxf v4.1.3.tar.gz
      #     cp swagger/* swagger-ui-4.1.3/dist/
      #     cd swagger-ui-4.1.3/dist/
      #     aws s3 sync . s3://colkie-swagger-production
      #   env:
      #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
